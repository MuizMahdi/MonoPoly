package helpers

import (
	"bufio"
	"fmt"
	"io"
	"io/ioutil"
	"os"
)

type Stage struct {
	Name        string
	Description string
	Actors      []Actor
}

type Actor struct {
	Name        string
	Description string
}

var WorkspacesTypes = newWorkspaceType()

func newWorkspaceType() *WorkspaceType {
	return &WorkspaceType{
		Stage: "stage",
		Actor: "actor",
	}
}

type WorkspaceType struct {
	Stage string
	Actor string
}

var ActorsStartIdicator = "## Stage Actors Start ##"
var ActorsEndIdicator = "## Stage Actors End ##"

// Reads lines into a string array from a given file.
func ReadLines(path string) ([]string, error) {
	file, err := os.Open(path)
	if err != nil {
		return nil, err
	}
	defer file.Close()

	var lines []string
	scanner := bufio.NewScanner(file)
	for scanner.Scan() {
		lines = append(lines, scanner.Text())
	}
	return lines, scanner.Err()
}

// Writes the lines of the given string array into the given file.
func WriteLines(lines []string, path string) error {
	file, err := os.Create(path)
	if err != nil {
		return err
	}
	defer file.Close()

	w := bufio.NewWriter(file)
	for _, line := range lines {
		fmt.Fprintln(w, line)
	}
	return w.Flush()
}

func File2lines(filePath string) ([]string, error) {
	f, err := os.Open(filePath)
	if err != nil {
		return nil, err
	}
	defer f.Close()
	return LinesFromReader(f)
}

func LinesFromReader(r io.Reader) ([]string, error) {
	var lines []string
	scanner := bufio.NewScanner(r)
	for scanner.Scan() {
		lines = append(lines, scanner.Text())
	}
	if err := scanner.Err(); err != nil {
		return nil, err
	}

	return lines, nil
}

func InsertStringToFile(path, str string, index int) error {
	lines, err := File2lines(path)
	if err != nil {
		return err
	}

	fileContent := ""
	for i, line := range lines {
		if i == index {
			fileContent += str
		}
		fileContent += line
		fileContent += "\n"
	}

	return ioutil.WriteFile(path, []byte(fileContent), 0644)
}
